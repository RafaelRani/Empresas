import styled from 'styled-components';

export const Title = styled.div`
  margin: 30px 0px;
  font-size: 17px;
  font-weight: bold;
  padding: 10px 30px;
  background-color: rgba(0, 0, 0, 0.1);
  display: flex;
`;
